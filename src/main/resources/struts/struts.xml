<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE struts PUBLIC
    "-//Apache Software Foundation//DTD Struts Configuration 2.3//EN"
    "http://struts.apache.org/dtds/struts-2.3.dtd">
<struts>
    <!-- 定义 Struts 常量 -->

    <!-- 开启使用开发模式，详细错误提示  -->
    <constant name="struts.devMode" value="false" />

    <!-- 指定全局国际化资源文件 -->
    <constant name="struts.custom.i18n.resources" value="resources/content/Language"/>

    <!-- 指定每次请求到达，重新加载资源文件, 开发是: true , 部署时 false -->
    <constant name="struts.i18n.reload" value="true" />

    <!-- 指定每次配置文件更改后，自动重新加载 -->
    <constant name="struts.configuration.xml.reload" value="false" />

    <!-- 设置浏览器是否缓存静态内容 -->
    <constant name="struts.serve.static.browserCache" value="false" />

    <!-- 请求参数的编码方式 -->
    <constant name="struts.i18n.encoding" value="utf-8" />

     <!-- 文件上传最大值 -->
    <constant name="struts.multipart.maxSize" value="104857600" />

    <!-- 让struts2支持动态方法调用 
    <constant name="struts.enable.DynamicMethodInvocation" value="true" />
    -->

    <!-- 允许标签中使用表达式语法 -->
    <constant name="struts.tag.altSyntax" value="true" />

    <!-- 对于WebLogic,Orion,OC4J此属性应该设置成true 
    <constant name="struts.dispatcher.parametersWorkaround" value="false" />
    -->

    <!-- 默认后缀名 -->
    <constant name="struts.action.extension" value="do,action,html,," />

    <!-- 指定由spring负责action对象的创建 -->
    <constant name="struts.objectFactory" value="spring" />

    <!-- Struts 注解模式配置 Annotation -->
    <constant name="actionPackages" value="com.angejia.dw.web_service.modules" />

</struts>